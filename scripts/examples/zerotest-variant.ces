vis {
    title: "Test zero with an inhibitor replaced"
}

cap {
    ω tested
}

mul {
    ω tested -> zero,
    0 tested -> notzero,
    0 aux_off <- tested aux_on,
}

ces Main {
    { entry tested => notzero }
  + { entry => tested aux_on zero }
  + { tested aux_on => aux_off }
}
